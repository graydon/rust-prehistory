mod hello;
mod main;


// Target-OS module.

alt (target_os) {
  case ("win32") {
    mod os = "win32_os.rs";
  } else {
    mod os = "unix_os.rs";
  }
}

// General IO and system-services modules.

mod io;
mod sys;

// Built-in types support modules.

mod _int;
mod _u8;
mod _vec;
mod _str;
