type incrementer = lim ^(pure fn (int i) : even(i) -> int : odd(*), lt(i,*));

fn! bar (zug[a,b,c] z, int i) -> nat
{
        y.(5) = x.(3 + e.(12) >> 2).z;
        x.(10) <- p.q;
        put 10;
        ret;
}


prog entry
{
        val sys.put.ty put;

        init(^sys.rt rt, ^vec[str] args) -> nil
        {
        }

        fn foo(int@ x) -> ()
        {	
            val int y;
            val foo bleh;
            putstr("Hello, world\n");
            x = x + 200 / 5;
            while (x < 60)
            {
                putstr("counting: ");
                putint(x);
                putstr("\n");
                if ((x % 2) == 0)
                {
                        putstr("ooh, an even number!\n");
                }
            x = x + 1;
            }
        }

        main
        {
                foo(10);
        }

        fini
        {
        }
}

